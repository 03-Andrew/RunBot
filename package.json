{
  "name": "runbot",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm --prefix lambdas/health run build",
    "terraform:init": "terraform -chdir=infrastructure init",
    "terraform:apply": "terraform -chdir=infrastructure apply"
  }
}
